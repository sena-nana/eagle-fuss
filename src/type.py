"""Eagle 素材库类型定义模块。

本模块定义了 Eagle 素材库中使用的核心类型别名，
用于提高代码可读性和类型安全性。
"""

type ID = str
"""Eagle 资源的唯一标识符。

每个素材和文件夹都有一个唯一的 ID，由 Eagle 应用生成。
ID 通常是一个由字母和数字组成的字符串，长度约为 13 个字符。

Examples:
    - "M1BXZA0YVSTZZ" - 素材 ID
    - "M9HAHF1P1W52R" - 文件夹 ID
    - "MAQI58XZ1DTQ5" - 另一个文件夹 ID
"""

type Stem = str
"""文件名主干（不含扩展名）。

表示文件的基本名称，不包含文件扩展名部分。
在 Eagle 素材库中，素材的显示名称即为此类型。

Examples:
    - "游戏编程模式" - PDF 文件的主干名
    - "ブルーアーカイブ オフィシャルアートワークス" - 图片文件的主干名
"""
